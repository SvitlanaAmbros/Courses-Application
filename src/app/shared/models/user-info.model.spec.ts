import {UserInfo} from '@shared/models/user-info.model';
import {User} from '@shared/models/user.model';

describe('User info',  () => {
  const userInfo: User = new UserInfo('1', 'john', 'doe');
});
